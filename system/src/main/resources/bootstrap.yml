spring:
  main:
    allow-bean-definition-overriding: true
  #配置中心  
  cloud:
     bus:
        trace:
           enabled: true
     config:
        uri: http://127.0.0.1:8001
        name: system
        profile: local