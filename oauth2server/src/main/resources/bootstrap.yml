spring:
  main:
    allow-bean-definition-overriding: true
  #配置中心  
  cloud:
     bus:
        trace:
           enabled: true
     config:
        uri: http://localhost:8002
        name: oauth2
        profile: dev